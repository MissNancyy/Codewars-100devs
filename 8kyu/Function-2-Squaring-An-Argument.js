// PROMPT: Now you have to write a function that takes an argument and returns the square of it.

// MY SOLUTION: 
function square (argument) {
    return Math.pow(argument,2)
  }
  console.log (square(5)) 

// OR 
function square (argument) {
    return argument*argument
  }
  console.log (square(5))

// OR
const square = (n) => n * n; 

// OR 
function square(x) {
    return x ** 2;
  }